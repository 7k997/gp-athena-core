import { KEY_BINDS } from '@AthenaShared/enums/keyBinds';

export const DEATH_CONFIG = {
    // Use default Key from KEY_BINDS. (E)
    // Or add new Key to KEY_BINDS and use it here.
    // Or use any other Key you want directly here (NOT RECOMMENDED, because no overview of used keys)
    RESPAWN_KEY: KEY_BINDS.INTERACT, // E
};
